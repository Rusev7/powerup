import Navigation from '../Navigation';
import MainSection from '../MainSection';

const Home = props => {
    return (
        <div>
            <Navigation />
            <MainSection />
        </div>
    )
}

export default Home;